<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <!-- Primeira "camada", com o ImageView que vai ficar centrado ao fundo -->
    <!--
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="bottom"
        android:scaleType="centerCrop"
        android:src="@drawable/mainbg"
        />
     -->

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:orientation="vertical"
        tools:context="br.com.fatecpg.brasileirao2016pdm.MainActivity">
        <!-- android:background="#4d000000" -->

        <Button
            android:layout_width="match_parent"
            android:layout_height="59dp"
            android:text="Verificar placar"
            android:onClick="forceUpdate"
            android:background="@drawable/btn"
            android:textSize="30dp"
            android:textAllCaps="false"
            android:textColor="#02143b"/>
        <!-- android:background="#dd0ac60a" -->

        <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_marginTop="5dp"
            android:id="@+id/listView"
            />
        <!--android:textColor="#ff0000"
            android:background="#5a000000"-->

    </LinearLayout>

</FrameLayout>
